<%= form_with(model: comment, local: true) do |form| %>

  <div class="field">
    <%= form.text_area :content, class: "form-control area" %>
  </div>

  <div class="actions">
    <%= form.submit%>
  </div>
<% end %>
