<div class="container">
<br>
    <h3 class="fw-light mb-3">Retrouvez tous les derniers articles : <h3>
</div>
<div class="container">
    <div class="row gy-3">
        <% @posts.each do |post| %>
            <div class="col-sm-4 d-flex align-items-stretch">
                <div class="card h-100" >
                  <%= link_to post_path(post.id) do %>
                        <%= image_tag"blog_image#{post.id}.webp", :alt => "", :class => "card-img-top", :style => "display: block; height: 300px; object-fit: cover; object-position: center;"%>
                  <% end %>
                    <div class="card-body row d-flex align-items-stretch">
                        <h3 class="display-7"><%= post.title %></h3>
                        <p class="card-text"><%=  truncate(post.description, length: 100) %></p>
                        <div class="d-flex justify-content-between align-items-end">
                            <div class="btn-group">
                            <a class="btn btn-primary stretched-link" href="<%=post_path(post.id)%>">Détails</a>
                            </div>
                            <small class="text-muted"><%= post.created_at.strftime("%d/%m/%Y") %></small>
                        </div>
                    </div>
                </div>
            </div>
        <% end %>
    </div>
</div>
